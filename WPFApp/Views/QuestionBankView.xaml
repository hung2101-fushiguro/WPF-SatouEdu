<UserControl x:Class="WPFApp.Views.QuestionBankView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent" FontFamily="Segoe UI">
    <Grid Margin="10,0,10,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Bottom">
                <TextBlock Text="Ngân hàng Câu hỏi" FontSize="36" FontWeight="ExtraBlack" Foreground="#191c1d" Margin="0,0,0,10"/>
                <TextBlock Text="Quản lý kho câu hỏi trắc nghiệm cho tất cả các môn học của bạn." FontSize="16" Foreground="#6b7280"/>
            </StackPanel>

            <StackPanel Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,15" HorizontalAlignment="Right">
                    <TextBlock Text="Lọc theo:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <ComboBox ItemsSource="{Binding Subjects}" SelectedItem="{Binding SelectedSubject}" DisplayMemberPath="SubjectName" Width="150" Height="35" Margin="0,0,15,0" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Khối:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox ItemsSource="{Binding AvailableGrades}" SelectedItem="{Binding SelectedGradeLevel}" Width="80" Height="35" VerticalContentAlignment="Center"/>
                </StackPanel>

                <Button Command="{Binding AddQuestionCommand}" HorizontalAlignment="Right" Height="45" Padding="20,0" Background="#00FFFF" BorderThickness="0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="8" Background="{TemplateBinding Background}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" Foreground="White" FontSize="16" Margin="0,0,8,0" FontWeight="Bold"/>
                                    <TextBlock Text="Thêm Câu Hỏi Mới" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="20" BorderBrush="#e1e3e4" BorderThickness="1">
            <DataGrid ItemsSource="{Binding QuestionsList}" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" BorderThickness="0" RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding QuestionId}" Width="50" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Nội dung câu hỏi" Binding="{Binding Content}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Khối lớp" Binding="{Binding GradeLevel}" Width="80" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Đáp án đúng" Binding="{Binding CorrectOption}" Width="100" IsReadOnly="True"/>

                    <!-- Cột Thao tác với 2 nút Sửa và Xóa -->
                    <DataGridTemplateColumn Header="Thao tác" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <!-- Nút Sửa -->
                                    <Button Command="{Binding DataContext.EditQuestionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                            CommandParameter="{Binding}" 
                                            Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0" ToolTip="Sửa câu hỏi">
                                        <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" Foreground="#d97706" FontSize="16"/>
                                    </Button>

                                    <!-- Nút Xóa -->
                                    <Button Command="{Binding DataContext.DeleteQuestionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                            CommandParameter="{Binding}" 
                                            Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Xóa câu hỏi">
                                        <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" Foreground="#dc2626" FontSize="16"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>