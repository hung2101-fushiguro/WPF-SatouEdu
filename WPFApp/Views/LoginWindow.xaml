<Window x:Class="WPFApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login | LMS Core" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#f8f9fa"
        FontFamily="Segoe UI"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <ScrollViewer x:Name="PART_ContentHost"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <ScrollViewer x:Name="PART_ContentHost"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="#ffffff" ClipToBounds="True">

            <Ellipse Width="500" Height="500" Fill="#bde9ff" Opacity="0.3" Margin="0,-200,-250,0" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Ellipse.Effect>
                    <BlurEffect Radius="100"/>
                </Ellipse.Effect>
            </Ellipse>
            <Ellipse Width="400" Height="400" Fill="#43da00" Opacity="0.1" Margin="-150,0,0,-150" HorizontalAlignment="Left" VerticalAlignment="Bottom">
                <Ellipse.Effect>
                    <BlurEffect Radius="80"/>
                </Ellipse.Effect>
            </Ellipse>

            <Grid VerticalAlignment="Top" Margin="40,30,40,0">
                <TextBlock Text="Satou Edu" FontSize="24" FontWeight="ExtraBlack" Foreground="#191c1d" HorizontalAlignment="Left"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Help" FontSize="14" FontWeight="Medium" Foreground="#6d797f" Margin="0,0,20,0" Cursor="Hand"/>
                    <TextBlock Text="Privacy" FontSize="14" FontWeight="Medium" Foreground="#6d797f" Cursor="Hand"/>
                </StackPanel>
            </Grid>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">

                <TextBlock Text="Xin chào!" FontSize="36" FontWeight="ExtraBlack" Foreground="#191c1d" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="Chúc bạn một ngày học tập thật hiệu quả." FontSize="14" FontWeight="Medium" Foreground="#5f5e5e" HorizontalAlignment="Center" Margin="0,0,0,40"/>

                <Border Background="#ffffff" CornerRadius="12" Padding="30" BorderBrush="#e1e3e4" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="40" ShadowDepth="12" Color="Black" Opacity="0.04"/>
                    </Border.Effect>
                    <StackPanel>

                        <TextBlock Text="Email" FontSize="13" FontWeight="SemiBold" Foreground="#3d484e" Margin="4,0,0,6"/>
                        <Border Background="#f3f4f5" CornerRadius="8" Height="48" Margin="0,0,0,20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="45"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="&#xE715;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#6d797f" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBox Text="{Binding Email}" Grid.Column="1" FontSize="14" Foreground="#191c1d" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center"/>
                            </Grid>
                        </Border>

                        <Grid Margin="4,0,4,6">
                            <TextBlock Text="Mật khẩu" FontSize="13" FontWeight="SemiBold" Foreground="#3d484e" HorizontalAlignment="Left"/>
                            <TextBlock Text="Quên mật khẩu?" FontSize="12" FontWeight="Bold" Foreground="#006683" HorizontalAlignment="Right" Cursor="Hand"/>
                        </Grid>
                        <Border Background="#f3f4f5" CornerRadius="8" Height="48" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="45"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="45"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="&#xE72E;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#6d797f" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <PasswordBox x:Name="txtPass" Grid.Column="1" FontSize="14" Foreground="#191c1d" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="&#xE890;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#6d797f" VerticalAlignment="Center" HorizontalAlignment="Center" Cursor="Hand"/>
                            </Grid>
                        </Border>

                        <CheckBox Content="Ghi nhớ đăng nhập" Foreground="#5f5e5e" FontWeight="Medium" FontSize="13" Margin="4,0,0,25">
                            <CheckBox.LayoutTransform>
                                <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                            </CheckBox.LayoutTransform>
                        </CheckBox>

                        <Button Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=txtPass}" Height="50" Cursor="Hand" BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border CornerRadius="8">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#006683" Offset="0"/>
                                                <GradientStop Color="#33ccff" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock Text="Đăng nhập" FontSize="15" FontWeight="Bold" Foreground="#ffffff" Margin="0,0,10,0"/>
                                            <TextBlock Text="&#xE72A;" FontFamily="Segoe MDL2 Assets" FontSize="14" FontWeight="Bold" Foreground="#ffffff" VerticalAlignment="Center" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Command="{Binding CloseCommand}" Height="40" Margin="0,15,0,0" Cursor="Hand" Background="Transparent" BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border CornerRadius="8" Background="#e5e2e1">
                                        <TextBlock Text="Thoát ứng dụng" FontSize="13" FontWeight="Bold" Foreground="#5f5e5e" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#004d64" Offset="0"/>
                    <GradientStop Color="#65d3ff" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="SATOU EDU" FontSize="50" FontWeight="Black" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Nền tảng quản lý học tập thông minh" FontSize="18" Foreground="#e1e3e4" Margin="0,10,0,0"/>
            </StackPanel>

            <Border>
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                        <GradientStop Color="#ffffff" Offset="0"/>
                        <GradientStop Color="#80ffffff" Offset="0.3"/>
                        <GradientStop Color="#00ffffff" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>
        </Grid>
    </Grid>
</Window>